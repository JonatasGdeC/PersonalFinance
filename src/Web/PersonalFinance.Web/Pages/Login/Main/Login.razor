@using PersonalFinance.Web.Pages.Login.Components.FormForgotPassword
@using PersonalFinance.Web.Pages.Login.Components.FormLogin
@using PersonalFinance.Web.Pages.Login.Components.FormRegisterUser
@using PersonalFinance.Web.Resources.Common
@using PersonalFinance.Web.Resources.Login

<PageTitle>@CommonResources.AppTitlePrefix@(_showPageLogin ? LoginResources.Login : LoginResources.SignUp)</PageTitle>

<section class="login">
    <div class="login__header">
        <img src="icons/Logo.svg" width="120" height="22" alt="@CommonResources.FinanceAppAlt"/>
    </div>

    <div class="login__set-language">
        <AddSetLanguage/>
    </div>

    <div class="login__sidebar">
        <img src="icons/Logo.svg" width="120" height="22" alt="@CommonResources.FinanceAppAlt"/>
        <div class="login__sidebar-text">
            <AddText Text="@LoginResources.SidebarTitle"
                     Preset="TextPreset.Preset1" Color="ThemeColor.White"/>

            <AddText Text="@LoginResources.SidebarSubtitle"
                     Preset="TextPreset.Preset4" Color="ThemeColor.White"/>
        </div>
    </div>

    <AddCard Width="var(--width-card-login)" Padding="var(--padding-card-login)">
        <Body>
        @if (_showPageLogin)
        {
            <FormLogin NavigateToSignUp="@ShowPageRegister" NavigateToForgotPassword="@ShowPageForgotPassword"/>
        }

        @if (_showPageRegister)
        {
            <FormRegisterUser NavigateToLogin="@ShowPageLogin"/>
        }

        @if (_showPageForgotPassword)
        {
            <FormForgotPassword NavigateToLogin="@ShowPageLogin"/>
        }
        </Body>
    </AddCard>
</section>
